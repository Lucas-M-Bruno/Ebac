function multiplicar(x:number, y:number):number {
    return x * y;
}

console.log(multiplicar(5,5));


function concatenar(nome: string): string {
    return "ola "+ nome;
}

console.log(concatenar("lucas"));